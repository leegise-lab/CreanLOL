<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="400dp"
    android:layout_height="500dp"
    android:layout_gravity="center"
    android:background="#EEEEEE">

    <ImageView
        android:id="@+id/setImageView"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:src="@android:drawable/ic_input_add"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"/>

    <TextView
        android:id="@+id/oqoq"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="(사진 등록 후 인식버튼을 눌러주세요)"
        android:textSize="18dp"
        app:layout_constraintTop_toTopOf="@id/orcbt"
        app:layout_constraintBottom_toBottomOf="@id/orcbt"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/orcbt"
        android:textColor="#5D5D5D"/>
    <Button
        android:id="@+id/orcbt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:gravity="center"
        android:onClick="processImage"
        android:text="텍스트 인식"
        app:layout_constraintTop_toBottomOf="@id/setImageView"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginRight="10dp" />

    <ScrollView
        android:id="@+id/scroll"
        android:layout_width="300dp"
        android:layout_height="200dp"
        app:layout_constraintHorizontal_bias="0.49"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/orcbt">

        <TextView
            android:id="@+id/OCRTextView"
            android:layout_width="300dp"
            android:layout_height="200dp"
            android:layout_marginTop="20dp"
            android:background="@drawable/edge"
            android:text="여기에 결과가 출력됩니다. 이름과 생년월일이 정확하게 인식되면 완료버튼을 눌러주세요."
            android:textColor="#169cdf"
            android:textSize="20dp"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/scroll" />
    </ScrollView>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toTopOf="@id/finishbt"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:textColor="#5D5D5D"
        android:text="이름과 생년월일이 일치하면 완료버튼을 눌러주세요."/>

    <Button
        android:id="@+id/finishbt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/scroll"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:text="완료"
        android:textColor="#000000"/>

</androidx.constraintlayout.widget.ConstraintLayout>